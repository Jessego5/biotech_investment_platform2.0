[pytest]
# put backend/ on sys.path so the tests can `from app... import ...` the same way
# the API and the scripts do, plus the lambda folder, whose modules are deployed
# flat and so are imported by bare name.
pythonpath = . ../infra/lambdas
testpaths = tests
