# keep the build context to the frontend's own source

node_modules
.next
.git
.env
.env.local

# the Python service ships from backend/Dockerfile with its own context
backend
infra
frontend
design
logs

*.db
*.db.bak-*
*.log
.DS_Store

# database dumps taken for a restore elsewhere. Large, and never part of an
# image or a commit, a 1 GB backup in the build context already broke one
# build with "no space left on device".
dumps/
*.dump
